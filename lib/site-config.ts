// Placeholder brand name — update once the domain is picked, then this is the
// only place that needs to change.
export const siteConfig = {
  name: "Seller Finance Homes",
  tagline: "Own a home in the OKC metro — no bank required.",
  phone: process.env.NEXT_PUBLIC_CONTACT_PHONE || "",
  leadFormUrl: process.env.NEXT_PUBLIC_LEAD_FORM_URL || "",
  serviceArea: "Oklahoma City metro",
};
